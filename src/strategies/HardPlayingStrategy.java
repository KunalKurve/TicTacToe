package strategies;

import models.Board;
import models.Move;
import models.Player;

public class HardPlayingStrategy implements BotsPlayingStrategy{
    @Override
    public Move makeMove(Board board, Player player) {
        return null;
    }
}
